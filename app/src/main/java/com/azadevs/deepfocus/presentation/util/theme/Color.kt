package com.azadevs.deepfocus.presentation.util.theme

import androidx.compose.ui.graphics.Color

// Modern Vibrant Focus Colors (Primary - Focus Phase)
val PrimaryLight = Color(0xFFFF5252)   // Dynamic Coral Red (Light Mode)
val PrimaryDark = Color(0xFFFF6B6B)    // Soft Crimson Coral (Dark Mode)

// Modern Refreshing Short Break Colors (Secondary - Short Break Phase)
val SecondaryLight = Color(0xFF00B4D8) // Electric Teal Ocean (Light Mode)
val SecondaryDark = Color(0xFF38BDF8)  // Vibrant Sky Teal (Dark Mode)

// Modern Deep Reset Long Break Colors (Tertiary - Long Break Phase)
val TertiaryLight = Color(0xFF7209B7)  // Deep Royal Violet (Light Mode)
val TertiaryDark = Color(0xFFA78BFA)   // Soft Lavender Violet (Dark Mode)

// Background & Surface Colors (Sleek Slate Palette)
val BackgroundLight = Color(0xFFF8FAFC)     // Slate 50 (Crisp, clean light background)
val SurfaceLight = Color(0xFFFFFFFF)        // Pure White
val SurfaceVariantLight = Color(0xFFF1F5F9) // Slate 100 (Clean card background without muddy purple tint)
val OnBackgroundLight = Color(0xFF0F172A)   // Slate 900 (High contrast dark text)
val OnSurfaceVariantLight = Color(0xFF475569) // Slate 600 (Subtle secondary text)
val OutlineLight = Color(0xFFE2E8F0)        // Slate 200

val BackgroundDark = Color(0xFF0F172A)      // Slate 900 (Deep, modern dark background)
val SurfaceDark = Color(0xFF1E293B)         // Slate 800 (Rich dark card surface)
val SurfaceVariantDark = Color(0xFF1E293B)  // Slate 800
val OnBackgroundDark = Color(0xFFF8FAFC)    // Slate 50
val OnSurfaceVariantDark = Color(0xFF94A3B8)  // Slate 400
val OutlineDark = Color(0xFF334155)         // Slate 700

val OnPrimary = Color.White
val Error = Color(0xFFEF4444)               // Modern Emerald Red Error